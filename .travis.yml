jobs:
  include:
    - arch: ppc64le
      dist: xenial
      group: edge
    - arch: ppc64le
      dist: bionic
      group: edge
    - arch: ppc64le
      dist: focal
      group: edge
   



script:
  - uname -a
  - cat /etc/hosts
  
