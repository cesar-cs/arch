jobs:
  include:
    - arch: ppc64le
      dist: xenial
      group: test
    - arch: ppc64le
      dist: bionic
      group: test
    - arch: ppc64le
      dist: focal
      group: test
   

language: java
jdk: openjdk8

script:
  - sudo apt list --installed
  - dpkg -l
