jobs:
  include:
    - arch: arm64-graviton2
      os: linux
      dist: jammy
      group: edge
      arch: arm64-graviton2
      virt: lxd

      
script:
  - cat /etc/os-release
  - cat /etc/hosts
  - uname -a
  - cat /proc/cpuinfo
  - curl icanhazip.com


  
  
