jobs:
  include:
    - arch: ppc64le
      dist: xenial
      group: stable
    - arch: ppc64le
      dist: bionic
      group: stable
    - arch: ppc64le
      dist: focal
      group: stable
   

language: java
jdk: openjdk8

script:
  - sudo apt list --installed
  - dpkg -l
